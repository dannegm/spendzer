import { remsInt } from '@/utils/helpers/styles';

const navitationHeaderStyles = {
  headerStyle: {},
  headerTitleStyle: {
    fontFamily: 'Montserrat_500Medium',
    fontSize: remsInt(1.2),
    fontWeight: 'normal',
  }
};

export default navitationHeaderStyles;
